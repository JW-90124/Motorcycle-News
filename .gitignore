node_modules/
.env
*.log
baoyu-skills/chrome-profile/
.claude/settings.local.json
dist/

# data/state.json and data/raw/*.json are intentionally NOT ignored — they
# must be committed so dedup state and collected signals persist across
# GitHub Actions' ephemeral runners.
